# Ghim cứng version cho pipeline fine-tune v3 (train_v3.py).
#
# VÌ SAO GHIM: train_v2.py chết trên Colab mới vì API thư viện trôi
# (trl bỏ `SFTTrainer(tokenizer=...)`). Colab luôn cài bản mới nhất nếu
# không ghim -> script hỏng dần theo thời gian mà không ai đụng vào code.
#
# Cài:  pip install -r offline_training/requirements_training.txt

transformers==4.51.3     # Qwen3 cần >= 4.51
peft==0.15.2
datasets==3.5.0
accelerate==1.6.0
# >=0.46.1: liger-kernel keo theo ban transformers moi hon, va ban do TU CHOI
# chay 4-bit voi bitsandbytes cu ("requires bitsandbytes>=0.46.1"). Ghim 0.45.5
# lam gay buoc nap model.
bitsandbytes>=0.46.1
openai==1.75.0           # client DeepSeek (base_url api.deepseek.com)

# BAT BUOC cho Qwen3: tu vung 151.936 token -> ham loss cua transformers lam
# logits.float() tao tensor (seq x 151936) roi NHAN DOI len fp32 -> OOM giua
# chung khi gap mau dai. Liger gop lm_head + cross-entropy thanh mot kernel,
# khong bao gio dung nguyen tensor logits.
#
# KHONG ghim cung: liger bam sat transformers, ghim lech ban la gay. Cai chung
# mot lenh voi bitsandbytes de pip giai quyet phu thuoc mot lan.
liger-kernel

# CÀI RIÊNG khi cần (không để chung vì nặng / dễ xung đột):
#   Bước quantize : pip install autoawq==0.2.9
#     (autoawq có thể đòi transformers khác — chạy sau khi train xong,
#      restart runtime; hoặc dùng llm-compressor, xem README)
#   Bước benchmark: pip install vllm==0.8.5
