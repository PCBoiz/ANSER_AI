# Python
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
.venv/
venv/

# Environment and secrets
.env
.env.*
secrets/ai_config.json


# Local data and model artifacts
src/models/
src/data/*.db
src/data/**/*.db
*.pt
*.bin
*.safetensors

# Repo Body kéo về để tham khảo — CÓ .git RIÊNG, không được commit vào Brain.
# Git sẽ coi thư mục này là gitlink và push lên một tham chiếu rỗng, vô dụng.
# Body là repo độc lập, xem AGENTS.md §1 (ranh giới repo).
/ANSER/

# Build and IDE
dist/
build/
.DS_Store
.idea/
.vscode/
*.bak
checkpoints/
models/
src/data/*.jsonl
offline_training/*.jsonl
offline_training/product_catalogs.json
*.pyc
# Staging catalog tạm do make_n8n_pairs/benchmark_v3 tự dựng lại mỗi lần chạy
offline_training/generated/_catalog/

# Backup cuc bo — KHONG track trong git (moi ban backup nhan doi moi bi mat lo commit;
# chinh _backups da nhan mat khau Neon thanh 12 ban trong su co 27/07/2026)
/_backups/
